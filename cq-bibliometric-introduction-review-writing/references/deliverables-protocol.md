# 学术交付协议

综述和绪论分别输出审计MD、清洁MD、单文件离线HTML和引用RIS。HTML的Word按钮在浏览器中即时生成DOCX，PDF按钮调用打印界面；不生成预制DOCX，不加载CDN。

浏览器端Word导出使用A4、学术页边距、中文宋体/英文Times New Roman、1.5倍行距、标题层级和APA 7参考文献。RIS必须从审计稿实际record/source ID生成，与正文引用集合一致。

版权标识只出现在HTML与浏览器导出Word的页脚，不进入学术正文：`JWC💗XQ@Rednote drharry`。
